apiVersion: v1
kind: Secret
metadata:
  name: planning-secrets
type: Opaque
stringData:
  # NOTE: These are example values. Replace with actual secrets in production
  # Consider using sealed-secrets or external secret management
  database-url: "postgresql://aurora:aurora_password@postgres-planning:5432/aurora_planning"

